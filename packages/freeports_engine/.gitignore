# Generated by Cargo
# will have compiled files and executables
debug
target

# Compiled extension module built in-place by `maturin develop`/`cargo build` into
# `python/freeports_engine/` (the maturin mixed-layout placeholder package — see that
# directory's own `__init__.py` docstring) and, historically, next to it as the hand-copied
# `freeports-native` binary (see `python/freeports/_internals/cli/cmd.py`'s
# `_native_binary_path`).
*.so
python/freeports_engine/freeports-native
*.egg-info/