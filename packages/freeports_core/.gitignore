# Generated by Cargo
# will have compiled files and executables
debug
target

# Compiled extension module built in-place by `maturin develop`/`cargo build` into
# `python/freeports/` (as the private `_native.<platform-tag>.so` submodule, per
# `agent-memory/maturin-idiomatic-restructure-plan.md`) and, next to it, the hand-copied
# `freeports-native` binary (see `python/freeports/_internals/cli/cmd.py`'s
# `_native_binary_path`).
*.so
python/freeports/freeports-native
*.egg-info/