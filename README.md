[![Freeports logo](https://www.freeports.org/assets/logo/complete-dark.svg)](https://www.freeports.org)
# Finance reports analysis ([Freeports](https://www.freeports.org))

![grants](validation/report/badges/grants-total.svg)
![coverage](validation/report/badges/grants-coverage.svg)
![check-grants](validation/report/badges/check-grants.svg)

![ci](ci/report/badges/ci-status.svg)
![tests.rust.lines](ci/report/badges/tests-rust-lines.svg)
![tests.python.lines](ci/report/badges/tests-python-lines.svg)
![docs.rust](ci/report/badges/docs-rust.svg)
![docs.python](ci/report/badges/docs-python.svg)
![lint.rust](ci/report/badges/lint-rust.svg)
![lint.python](ci/report/badges/lint-python.svg)

This project is intended parse finance pdf reports and create `CSV` dataset.
The purpose of the project and related infos can be found at the [*official website*](https://www.freeports.org).

## Installation
Two things are installed together and are easy to confuse: the `freeports` **command** and the
`freeports` **Python module**. They are two build products of one Rust crate, and neither implies
the other.

### From PyPI
```bash
pip install freeports
```

### From source
The repository's `Makefile` does the whole setup, virtual environment included:

```bash
git clone https://github.com/tvp-freeports/analysis_finance_reports.git
cd analysis_finance_reports
make install                  # the engine: extension and command
source venv/freeports-dev/bin/activate
make installcheck             # says nothing if both work
```

Building from source needs a Rust toolchain (`rustup`, stable channel) as well as Python ≥ 3.10.

One target per role, and `make help` lists the rest:

| You are | Run |
|---|---|
| using the engine | `make install` |
| working on the engine | `make dev-engine` |
| writing a PDF format | `make dev-formats` |
| writing documentation | `make dev-docs` |
| maintaining all of it | `make dev-all` |

`make doctor` reports what is installed, what is missing, and which target supplies it.

## Quickstart
```bash
freeports -h
```
shows the options; all of them can also be given as environment variables or in a configuration
file. To drive the engine as a library instead:

```python
import freeports
```

A working installation is not yet a working run: two inputs must exist first, and neither ships
with the engine — an **input database** of target companies and a **formats repository** with the
parsing definitions for the documents you have. See the
[full documentation](https://docs.freeports.org).

## What the documentation has been vouched for
The prose assertions under `docs/source/validation/assertions/` are statements about this software
that a person read, understood and agreed with — each one recorded as a signed grant under the
[agreement and good faith](https://docs.freeports.org/en/stable/validation/methodologies/agreement_and_good_faith.html)
methodology. This is what those grants add up to today.

<!-- freeports-validate:begin -->
<!-- Written by `freeports-validate report`. Anything between the markers is replaced. -->

1 grant by 1 contributor under 1 methodology; coverage 1/1 (100%); check-grants: passing

*Generated by `freeports-validate report`, and demonstrative: it shows what this repository's validation documents claim as of one run, not that the data is correct. To be sure of any line, run `freeports-validate check-grants` yourself.* [What these claims are worth: the general methodology](https://docs.freeports.org/en/stable/validation/general_methodology.html).

| Methodology | Grants | Coverage | Adopted by |
| --- | ---: | ---: | --- |
| [agreement and good faith](https://docs.freeports.org/en/latest/_sources/validation/methodologies/agreement_and_good_faith.rst.txt) | 1 | 1/1 (100%) | Oreste Sciacqualegni |

<!-- freeports-validate:end -->

The same claims are written out seven ways in the documentation, under
[Validation coverage](https://docs.freeports.org/en/latest/validation/report/index.html): the
summary above, and the three lookups in both of each one's groupings. The `pre-commit` hook
refreshes all of it, and `make validation-report` does the same by hand.

## What a commit here has to clear

`ci.yaml` at the root says how this repository is gated, and it is the only file to open to answer
that. Every figure in it is measured by a command you can re-run: the number that refuses a commit
is sitting in a file, never in the gate. `make branch-class` says which class the branch you are on
is in — on `dev`, the default, everything is measured and reported and nothing is refused; on
`prod` a missed threshold, a failing suite or a figure nobody could compute refuses the commit.

<!-- freeports-dev:begin -->
<!-- Written by `freeports-dev ci-report`. Anything between the markers is replaced. -->

12 metrics, 12 gated: 5 pass, 1 not measured, 6 measured at another commit; branch dev → dev (thresholds are reported, not enforced); ci-check: inconclusive

*Generated by `freeports-dev ci-report`, and demonstrative: it shows what one run measured on one machine, not that the software is correct. To be sure of any line, re-run the command that measures it and `freeports-dev ci-check` yourself.* [What is gated here, and what refuses: the commit gate](https://docs.freeports.org/en/latest/guides/devops/the-gate.html).

| Metric | Measured | Minimum | Verdict |
| --- | ---: | ---: | --- |
| `tests.rust.lines` | 91.2 % | 91.0 % | stale |
| `tests.python.lines` | 54.0 % | 32.0 % | stale |
| `docs.rust` | 39.4 % | 39.0 % | stale |
| `docs.python` | 55.9 % | 49.0 % | pass |
| `lint.rust` | 9.998 | 9.900 | pass |
| `lint.python` | 10.000 | 10.000 | pass |
| `grants.coverage` | — | 100.0 % | not measured |
| `grants.keys_online` | 100.0 % | 100.0 % | stale |
| `tests.python.freeports_dev.lines` | 52.5 % | 27.0 % | stale |
| `tests.python.freeports_validate.lines` | 89.4 % | 89.0 % | stale |
| `docs.python.freeports_dev` | 55.2 % | 47.0 % | pass |
| `docs.python.freeports_validate` | 64.0 % | 64.0 % | pass |

tests.rust.lines, docs.rust, grants.keys_online, tests.python.freeports_dev.lines, tests.python.freeports_validate.lines — measured at a03b5a5e, not at this one. Run `make ci` to take it again.

tests.python.lines — measured at a03b5a5e, not at this one. Run `make ci` to take it again. (packages with their own minimum: freeports_dev, freeports_validate)

grants.coverage — freeports-validate found no file a grant could cover, so there is no ratio to report. Run `freeports-validate collect` and check its output.

rust.integration — last run at a03b5a5e, not at HEAD (8f8217ee). Run `make test-rust-integration`.

rust.doc — last run at a03b5a5e, not at HEAD (8f8217ee). Run `make test-rust-doc`.

python.slow — last run at a03b5a5e, not at HEAD (8f8217ee). Run `make test-python-slow`.

python.online — last run at a03b5a5e, not at HEAD (8f8217ee). Run `make test-python-online`.

*pass* — measured, current, and at or above the minimum this repository asks for.

*not measured* — nobody could compute it — the tool is missing, or its run failed. Never a pass.

*stale* — computed, but at a commit other than this one, so it describes other code.

<!-- freeports-dev:end -->

The `pre-commit` hook refreshes the badges, the block above, the two pages beside it and the
single-page HTML rendering, and `make ci-report` does the same by hand. The same figures are written
out three ways in the documentation, under
[the continuous-integration report](https://docs.freeports.org/en/latest/dev/ci-report/index.html):
this summary, what each threshold is a threshold *of*, and where each figure comes from.

## Contributing
See [CONTRIBUTING.md](CONTRIBUTING.md), and the developer section of the
[documentation](https://docs.freeports.org) for the build system, the test conventions and the
translation workflow.
