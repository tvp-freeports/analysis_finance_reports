=================================
By file — under which methodology
=================================

The same view as ``freeports-validate who-grants -m <file>``, asked of the whole repository at once instead
of about the one file, contributor or methodology you would name on the command line.

.. freeports-validate:begin
.. Written by `freeports-validate report`. Anything between the markers is replaced.

**By file, listing the methodologies it was granted under**

The same view as ``freeports-validate who-grants -m <file>``, for the whole repository at once.

1 grant by 1 contributor under 1 methodology; coverage 1/1 (100%); check-grants: passing

*Generated by `freeports-validate report`, and demonstrative: it shows what this repository's validation documents claim as of one run, not that the data is correct. To be sure of any line, run `freeports-validate check-grants` yourself.*
`What these claims are worth: the general methodology <https://docs.freeports.org/en/stable/validation/general_methodology.html>`_.

==============================================================================  ========================  =======
File                                                                            Methodology               State
==============================================================================  ========================  =======
``docs/source/validation/assertions/validation_algorithm_trustworthiness.rst``  agreement and good faith  current
==============================================================================  ========================  =======

* agreement and good faith: https://docs.freeports.org/en/latest/_sources/validation/methodologies/agreement_and_good_faith.rst.txt

.. freeports-validate:end

Everything between the two markers above is **generated**, and is replaced whole every time the
report is refreshed — nothing written between them survives. The ``pre-commit`` hook rewrites it at
every commit; by hand it is

.. code-block:: sh

   freeports-validate report --format rst --table file-methodology \
       --out docs/source/validation/report/file-methodology.rst

or, for all seven pages, the badges and the README in one walk of the repository,
``make validation-report``.
